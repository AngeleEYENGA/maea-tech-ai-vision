# 1. Remplacer le contenu du fichier
cat > requirements.txt << 'EOF'
streamlit>=1.35.0,<2.0.0
ultralytics>=8.2.0
opencv-python-headless>=4.9.0
numpy>=1.24.0,<2.0.0
pandas>=2.0.0
plotly>=5.17.0
Pillow>=10.0.0
torch>=2.0.0
torchvision>=0.15.0
folium>=0.14.0
geopy>=2.3.0
streamlit-folium>=0.15.0
requests>=2.31.0
reverse-geocoder>=1.5.1
streamlit-js-eval>=0.1.0
matplotlib>=3.7.0
scipy>=1.10.0
EOF

# 2. Commit et push
git add requirements.txt
git commit -m "Fix requirements.txt - remove git conflict markers"
git push
